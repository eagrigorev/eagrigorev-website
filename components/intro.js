import style from './intro.module.scss';

const Intro = () => {
  return (
    <header className={style.intro}>
      <h1>Software developer & artist based in Thessaloniki, Greece</h1>
    </header>
  );
};

export default Intro;
